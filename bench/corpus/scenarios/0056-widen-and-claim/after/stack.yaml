apiVersion: security.istio.io/v1
kind: RequestAuthentication
metadata: { name: billing-api, namespace: prod }
spec:
  selector: { matchLabels: { app: billing-api } }
  jwtRules:
    - issuer: "https://kc/realms/main"
      audiences: ["billing-api", "billing-batch"]
