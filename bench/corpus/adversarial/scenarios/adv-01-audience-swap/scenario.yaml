# Hard: one audience removed AND one added at once. A net-set change must still
# surface the widening (api-v2 newly accepted), not cancel out.
id: "adv-01"
name: audience-swap
before: before
after: after
expected: { detected: true, class: widened, severity: medium, consumer: "k8s://local/prod/api-a" }
