apiVersion: security.istio.io/v1
kind: RequestAuthentication
metadata: { name: billing, namespace: prod }
spec:
  selector: { matchLabels: { app: billing } }
  jwtRules:
    - issuer: "https://kc/realms/main"
      audiences: ["billing", "billing-legacy"]
