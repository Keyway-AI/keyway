apiVersion: security.istio.io/v1
kind: RequestAuthentication
metadata:
  name: api-a
  namespace: prod
  annotations:
    argocd.argoproj.io/sync-wave: "2"
    kubernetes.io/change-cause: "chore: bump sidecar (PR #911)"
spec:
  selector: { matchLabels: { app: api-a } }
  jwtRules:
    - issuer: "https://kc/realms/main"
      audiences: ["api-a"]
