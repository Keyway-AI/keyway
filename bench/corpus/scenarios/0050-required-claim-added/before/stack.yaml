apiVersion: security.istio.io/v1
kind: RequestAuthentication
metadata: { name: orders-api, namespace: prod }
spec:
  selector: { matchLabels: { app: orders-api } }
  jwtRules:
    - issuer: "https://kc/realms/main"
      audiences: ["orders-api"]
