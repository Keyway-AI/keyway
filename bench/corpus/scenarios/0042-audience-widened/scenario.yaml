# True positive: an audience is added to api-a's Istio policy. Discovery must
# find api-a in both states and the diff must classify the change as `widened`.
id: "0042"
name: audience-widened
before: before
after: after
expected:
  detected: true
  class: widened
  severity: medium
  consumer: "k8s://local/default/api-a"
