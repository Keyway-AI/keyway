# No-op / true negative: only an owner label changes (a redeploy with a bumped
# dependency). Discovery sees it, but the contract is unchanged, so the diff must
# emit ZERO events (AC-8). Without the no-op half of the corpus the FPR is
# meaningless (PRD §13.2).
id: "0201"
name: dependency-bump-noop
before: before
after: after
expected:
  detected: false
  consumer: "k8s://local/default/api-a"
