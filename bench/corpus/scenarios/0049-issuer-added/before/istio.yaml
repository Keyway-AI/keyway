apiVersion: security.istio.io/v1
kind: RequestAuthentication
metadata: { name: api-x, namespace: prod }
spec:
  selector: { matchLabels: { app: api-x } }
  jwtRules:
    - issuer: "https://kc/realms/main"
      audiences: ["api-x"]
